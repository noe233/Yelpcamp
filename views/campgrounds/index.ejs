<% layout('/layout/boilerPlate') %>
    <div id="map" style="width:100%; height:500px"> </div>
    <h1>All Campgrounds</h1>
    <div>
        <a href="/campgrounds/new">Add Campground</a>
    </div>
    <div><% for (let campground of campgrounds){ %>
        <div class="card mb-3" >
            <div class="row g-0">
              <div class="col-md-4">
                <img src="<%= campground.image.url %>" class="img-fluid rounded-start" alt="...">
              </div>
              <div class="col-md-8">
                <div class="card-body">
                  <h5 class="card-title"><%= campground.title %></h5>
                  <p class="card-text"> <%= campground.description  %></p>
                
<a class="btn btn-secondary"  href="/campgrounds/<%= campground._id%>">View campground</a>
                  
                </div>
              </div>
            </div>
          </div>
    <% } %>
    </div>
    <script> 
    const mapToken = '<%-process.env.mapbox_token%>'
    const campgrounds = {features: <%- JSON.stringify(campgrounds)%>};
    </script>
   <script src="/javascripts/clusterMap.js">
    
   </script> 
   
    
    
    <!-- <ul>
        <% for (let campground of campgrounds){ %>
            <li>
                <a href="/campgrounds/<%= campground._id%>">
                    <%= campground.title %>
                </a>
            </li>
            <% } %>
    </ul> -->

